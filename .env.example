# Gmail: no env var needed — see README for OAuth setup
# (credentials.json + `automator gmail auth` generates token.json)

# Optional: override Ollama model
OLLAMA_MODEL=qwen2.5

# Optional: override Ollama base URL
OLLAMA_BASE_URL=http://localhost:11434
